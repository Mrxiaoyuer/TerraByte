# CLINE dependencies
memory-bank/
doc/

# Node dependencies
node_modules/
**/node_modules/

# Next.js build output
.next/
*/.next/
out/
build/

# Local env files (do not commit secrets)
.env
.env.*.local
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
logs/
*.log

# OS files
.DS_Store
Thumbs.db

# Editor directories and files
.vscode/
.idea/
*.sublime-workspace
*.sublime-project

# TypeScript
*.tsbuildinfo

# Temporary or build folders
dist/
coverage/
.cache/
.temp/
tmp/
temp/

# Uploaded user files (if uploads are generated at runtime)
public/uploads/
uploads/
geotest/public/uploads/
geotest/uploads/

# Misc
*.local
*.swp
*.pid
*.seed

# Mac/Unix swap files
*~
.*.swp

# Optional: lockfiles for alternative package managers (uncomment if you want to ignore)
# pnpm-lock.yaml
# yarn.lock

# Keep track: if files are already committed, they will remain tracked until removed from the index.
# To untrack an already committed file/folder, run:
# git rm -r --cached path/to/file-or-folder

# -------------------------
# Additional ignores
# -------------------------

# Python virtualenvs and caches (commonly created in microservices or local dev)
venv/
.venv/
env/
.env/           # keep secret env files ignored (already listed above, repeated for clarity)
ENV/
.python-version
pip-wheel-metadata/
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.pyc

# Python packaging & test artifacts
*.egg-info/
*.egg
.eggs/
build/
dist/
pip-log.txt
pip-delete-this-directory.txt
.python-version
__pypackages__/

# Python tooling caches
.mypy_cache/
.pytest_cache/
.tox/
.coverage
.cache/
htmlcov/

# Virtualenv created by some IDEs
venv.bak/
virtualenv/
venv-win/
.venv-win/

# Editor backup/temp files
*~
*.swp
*.swo
*.bak
*.orig

# IDE specific
.vscode/
.idea/
*.iml

# Runtime files
*.pid
*.seed

# JetBrains
/out/
.idea/

# macOS
.DS_Store

# Windows
Thumbs.db

# Notes:
# - If files were already committed, .gitignore won't untrack them automatically.
#   To untrack files already staged/committed (e.g., accidental git add .), run:
#     git restore --staged .
#   to unstage all files, or:
#     git rm -r --cached path/to/file-or-folder
#   to remove committed files from the index while keeping them locally.
#
# -------------------------
# Project-specific additions
# -------------------------
# Ignore user Azure config if accidentally placed in the repo root or project:
# This is defensive: the canonical place is the user's home (~/.azure/), but if a local copy
# is created in the repository, this prevents accidental commits.
/.azure/
# Explicitly ignore any user-level Azure config file if placed in repo
/.azure/gpt-4o-mini.config

# Ignore local run scripts or temp files you might create during testing
local-scripts/
*.localenv

# End of .gitignore
